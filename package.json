{
  "name": "btpdevops",
  "version": "1.0.0",
  "private": true,
  "description": "BTP DevOps workshop sample application",
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "mbt build -t gen --mtar archive",
    "deploy": "cf deploy gen/archive.mtar --retries 0 --delete-services",
    "undeploy": "cf undeploy btpdevops --delete-services",
    "format": "oxfmt",
    "ci-lint": "cds lint",
    "test": "node --test"
  },
  "dependencies": {
    "@cap-js/sqlite": "^2.1.3",
    "@sap/cds": "^9.7.1",
    "@sap/xssec": "^4.12.2"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.7.2",
    "mbt": "^1.2.45",
    "oxfmt": "^0.41.0",
    "@sap/eslint-plugin-cds": "^4",
    "eslint": "^9",
    "@cap-js/cds-test": "^0"
  },
  "cds": {
    "requires": {
      "destinations": true,
      "[production]": {
        "auth": "xsuaa"
      }
    }
  }
}
